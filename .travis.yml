dist: bionic

language: python
python:
  - "3.8"

services:
  - docker

before_script: pip install docker-compose

install: pip install codecov

script:
  - docker-compose run app sh -c "python manage.py test && flake8 -v"

after_success:
  - codecov